---
description: FACTORIA UI 模块的 View/Controller/Model 分工与依赖方向
globs: assets/src/ui/**/*.gd
alwaysApply: false
owner: client-ui
status: active
last_updated: 2026-05-13
version: 1.0.0
---

# UI 模块 View / Controller / Model 分工

## 适用目录

FACTORIA 当前有界面的 UI 模块统一放在：

- `assets/src/ui/<module>/view/*.gd`
- `assets/src/ui/<module>/core/*Controller.gd`
- `assets/src/ui/<module>/core/*Helper.gd`
- `assets/src/ui/<module>/model/*.gd`

不要沿用旧工程的 `assets/src/modules/**` 路径。

## 分层职责

- View：继承 `BaseUI`，负责节点引用、界面显示、用户输入收集。
- Controller：继承 `BaseController`，负责模块流程、跨系统调用、信号派发、存档参与接口。
- Model：继承 `BaseModel` 或 `RefCounted` 数据类，负责模块状态、快照、序列化数据。
- Helper：放模块内纯函数工具，不持有 UI 节点和运行时状态。

## 依赖方向

推荐方向：

`View -> Controller -> Model`

允许 Controller 调用：

- 本模块 Model / Helper。
- `ControllerManager` 获取必要的其它 Controller。
- 核心管理器，例如 `UIManager`、`SaveManager`。

禁止：

- View 直接改写 Model。
- View 直接读写磁盘、编排存档阶段或调用底层文件 API。
- Model 持有 UI 节点、Controller、SceneTree。
- 把模块特定 Helper 塞进 `BaseController`。

## 模块边界

模块按功能聚合，例如：

- `startgame`
- `settings`
- `save`
- `rolecreate`

无界面游戏业务模块不要放进 `assets/src/ui`，统一放进 `assets/src/game/<module>`。

后续 gameplay、planet、blueprint、agent、achievement 等不应强行放进 UI MVC；它们应建立独立的 game/planet 架构，再由 UI 调用其对外服务。
